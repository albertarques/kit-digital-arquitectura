<form method="post" action="{{ path('app_shipment_delete', {'id': shipment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shipment.id) }}">
    <button class="btn">Delete</button>
</form>
