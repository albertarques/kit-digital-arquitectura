<form method="post" action="{{ path('app_postal_adress_delete', {'id': postal_adress.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ postal_adress.id) }}">
    <button class="btn">Delete</button>
</form>
